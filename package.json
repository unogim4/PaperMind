{
  "name": "papermind",
  "version": "1.0.0",
  "description": "AI-powered academic paper research assistant that finds relevant papers and generates abstracts",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run mcp\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "mcp": "cd mcp-server && npm start",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../mcp-server && npm install",
    "build": "cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ai", "research", "papers", "academic", "arxiv", "claude", "mcp"
  ],
  "author": "unogim4",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/unogim4/PaperMind.git"
  }
}